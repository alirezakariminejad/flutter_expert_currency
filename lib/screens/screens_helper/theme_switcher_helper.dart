import 'package:flutter/material.dart';

class ThemeSwitcherHelper extends StatelessWidget {
  const ThemeSwitcherHelper({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(
      onPressed: () {},
      icon: Icon(Icons.sunny),
    );
  }
}
